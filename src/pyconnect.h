#ifndef PYCONNECT_H
#define PYCONNECT_H

/*
#include <QCoreApplication>
#include <Python.h>
#include <QDebug>

class PyConnect
{
    Q_OBJECT
public:
    explicit PyConnect(QWidget *parent = 0);
    ~PyConnect();

    // 加载python文件
    PyObject *pModule;

    // 加载文件目录
    PyObject* pDict;

    // 加载python类
    static PyObject* pClassCalc;

    // 加载构造函数
    PyObject* pConstruct;

    // 实例化类得到类对象
    PyObject* pInstance;

    // 调用对象的成员函数
    PyObject* pRet1;
};


*/



#endif // PYCONNECT_H
